
import { isActive } from 'julz/helpers/is-active';
import { module, test } from 'qunit';

module('Unit | Helper | is active');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isActive([42]);
  assert.ok(result);
});

